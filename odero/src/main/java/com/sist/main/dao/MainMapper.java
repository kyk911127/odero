package com.sist.main.dao;

import org.apache.ibatis.annotations.Select;

public interface MainMapper {
	//BEST FOOD
/*	@Select("SELECT p_img, p_name, p_addr, p_no "
			+ "FROM place "
			+ "WHERE p_grade='f' ")
	public PlaceVO best_food();*/
}
